jp_seismic:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'seismic') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'seismic') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'seismic') }}"
      schema: analytics
      threads: 4
      keepalives_idle: 0
