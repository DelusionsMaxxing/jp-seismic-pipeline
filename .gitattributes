# Normalise to LF in the repository. Shell scripts and SQL run inside Linux
# containers, where a CRLF shebang fails with "bad interpreter".
* text=auto eol=lf

*.sh    text eol=lf
*.sql   text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
Makefile text eol=lf
Dockerfile text eol=lf
